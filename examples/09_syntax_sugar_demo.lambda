; Syntax sugar demonstration
; This example shows both lambda and let-style function definitions

; Multi-parameter lambda syntax
let add_lambda = fun (x : Int) (y : Int) -> succ x in

; Let-style function definition (preferred for named functions)
let add_let (x : Int) (y : Int) = succ x in

; Both are equivalent - they desugar to nested lambdas
; Result: 6
add_let 5 0
